<%= form_for(@centrocosto) do |f| %>
  <% if @centrocosto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@centrocosto.errors.count, "error") %> prohibited this centrocosto from being saved:</h2>

      <ul>
      <% @centrocosto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre_centro %><br>
    <%= f.text_field :nombre_centro %>
  </div>
  <div class="field">
    <%= f.label :codigo_centro %><br>
    <%= f.text_field :codigo_centro %>
  </div>
  <div class="field">
    <%= f.label :detalle_centro %><br>
    <%= f.text_field :detalle_centro %>
  </div>
  <div class="actions">
    <%= f.submit ( f.object.new_record? ? "Crear" : "Actualizar"), class: "btn" %>
  </div>
<% end %>
